# Copy to .env and fill in values
# NPM token (read-only) for private scopes and higher rate limits
NPM_TOKEN=

# Libraries.io API key for better dependents coverage
LIBRARIES_IO_API_KEY=

# Optional overrides
# NPM_REGISTRY=https://registry.npmjs.org
# NPM_SEARCH_URL=https://api.npms.io/v2/search
# CONCURRENCY=8
